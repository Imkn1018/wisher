dones
<% @tag_list.each do |list| %>
  <% if list.wishes.where(isCompleted:true).count != 0  %>
     <p><%=link_to list.tag_name,wishes_dones_path(tag_id:list.id)%><%= list.wishes.where(isCompleted:true).count %></p>
  <% end %>
<% end  %>

<% if params[:tag_id]  %>
  <h1><%= @tag.tag_name %>のタグ</h1>
<% end  %>
<div class="d-flex flex-wrap justify-content-around">
<% @wishes.each do |wish| %>
  <div class="card p-4">
      <%= link_to wish_path(wish.id) do %>
          <div class="d-flex flex-column  align-items-center text-align-center pt-2">
            <div><%= attachment_image_tag wish, :wish_image , size:'200x200', class:"rounded-circle" %></div>
            <div class="pt-3 text-center text-info"><%= wish.wish_title %></div>
            <div class="pt-1"><%= wish.memo %></div>
            <div class="d-flex flex-wrap justify-content-around p-2">
                <% wish.tags.each do |wish_tag| %>
                    <div class="rounded-pill bg-light p-3 m-2">
                        <%= link_to wish_tag.tag_name, wishes_dones_path(tag_id: wish_tag.id) ,class:"text-secondary text-center"%>
                    </div>
                <% end %>
            </div>
          </div>
      <% end %>
    </div>
<% end %>
</div>
